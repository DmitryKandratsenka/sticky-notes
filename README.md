# Sticky Notes

A single-page sticky-notes board for desktop browsers, built with React and TypeScript from
scratch — no UI kits, no drag-and-drop or state libraries.

## Getting started

Requires Node.js 20.19+ (tested on Node 24).

```bash
npm install
npm run dev      # start the dev server
npm run build    # type-check and produce a production build in dist/
npm run preview  # serve the production build locally
npm run verify   # typecheck + lint + format check + tests
```

_Architecture description and feature guide will be completed alongside the implementation._
